#:kivy 2.2.0

<MirrorViewWidget>:
    # cols: 1
    # Label:
    #     text: 'Mirror view goes here.'

    diameter: self.height * 0.9
    canvas:
        Color:
            rgb: 1, 1, 1
        Ellipse:
            pos: 50, (self.height-self.diameter)/2
            size: self.diameter, self.diameter
            # size: 200 * wm.value, 201 * hm.value
            # source: 'data/logo/kivy-icon-512.png'
            # angle_start: e1.value
            # angle_end: e2.value

<MirrorViewControlPanel>:
    cols: 2
    MirrorViewWidget:
    GridLayout:
        cols: 1
        Button:
            text: '1'
        Button:
            text: '2'
        Button:
            text: '3'
        